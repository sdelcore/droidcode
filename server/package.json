{
  "name": "droidcode-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Companion metadata + event-mirror service for droidcode, runs alongside sandbox-agent.",
  "scripts": {
    "dev": "tsx watch --clear-screen=false src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "better-sqlite3": "^11.10.0",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.12.2",
    "tsx": "^4.21.0",
    "typescript": "~5.9.2"
  }
}
